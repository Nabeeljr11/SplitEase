{
  "version": 2,
  "installCommand": "npm install --include=dev",
  "buildCommand": "npx prisma generate && npm run build",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api"
    }
  ]
}
